  Available Commands for Future Use:
  - npm run db:purge - Interactive purge (prompts for confirmation)
  - npm run db:purge:force - Force purge without prompts
  - npm run db:purge:bash - Bash script with SQL backup