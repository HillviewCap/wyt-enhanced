# PostgreSQL Client Authentication Configuration File
# ===================================================
#
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            trust

# IPv6 local connections:
host    all             all             ::1/128                 trust

# Allow connections from the Kali host (adjust IP as needed)
# Temporary permissive rule for VMware NAT troubleshooting
host    isr_db          isr_user        0.0.0.0/0              md5
host    isr_db          isr_user        10.0.10.0/24           md5
host    isr_db          isr_user        10.0.10.9/32           md5
host    isr_db          isr_user        192.168.189.0/24       md5
host    isr_db          isr_user        100.76.119.65/32       md5
host    isr_db          isr_user        100.0.0.0/8            md5

# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            trust
host    replication     all             ::1/128                 trust
